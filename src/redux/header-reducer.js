const initialState = {
    headerInfo: {
        resume: "Resume",
        names: "STANISLAV KOVRIGIN"
    }
}

const headerReducer = (state = initialState, action) => {
    return state
}

export default headerReducer
