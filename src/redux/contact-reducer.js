const initialState = {
    contactInfo: {
        resume: "Resume",
        names: "STANISLAV KOVRIGIN"
    }
}

const contactReducer = (state = initialState, action) => {
    return state
}

export default contactReducer
